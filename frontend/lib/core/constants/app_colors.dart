import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Emerald & Mint)
  static const Color primary = Color(0xFF0D9488); // Deep Teal
  static const Color primaryLight = Color(0xFF2DD4BF);
  static const Color primaryDark = Color(0xFF115E59);

  // Secondary & Accents
  static const Color secondary = Color(0xFF10B981); // Emerald Green
  static const Color accent = Color(0xFF8B5CF6); // Soft Lavender
  static const Color warning = Color(0xFFF59E0B); // Amber / Meals
  static const Color info = Color(0xFF0EA5E9); // Sky Blue / Water
  static const Color sleep = Color(0xFF6366F1); // Indigo / Sleep
  static const Color error = Color(0xFFEF4444); // Coral Red
  static const Color success = Color(0xFF10B981);

  // Backgrounds & Neutrals
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color card = Colors.white;
  static const Color divider = Color(0xFFE2E8F0);

  // Typography
  static const Color textPrimary = Color(0xFF0F172A); // Slate 900
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
  static const Color textMuted = Color(0xFF94A3B8); // Slate 400

  // Chips & Badges
  static const Color chipBackground = Color(0xFFF1F5F9);
}
